syntax = "proto3";

package analyzer;
option go_package="./analyzer";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Analyzer {
  rpc Ping(Request) returns(Response);
}
